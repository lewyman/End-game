{
  "version": 2,
  "builds": [
    { "src": "frontend/dist/**", "dest": "/frontend/dist/**" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/frontend/dist/$1" }
  ]
}
